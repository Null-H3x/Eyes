# Field Report 158 — COMMUNITY CRITIQUE: TWO CLAIMS CONFIRMED, ONE INFERENCE BROKEN BY ITS OWN CONTROL

*July 2026. Cycle: EYESPIRAL-C. External critique, verified point by point.*

---

## CLAIM 1 — the W1@59 ↔ E3@90 fixed point. **CONFIRMED, and it explains a result I could not.**

```
West 1 @59 : [64, 1,30,73,23,67, 6,33,25,64,81,68,46,17,36,13,17,21,68]
East 3 @90 : [22,63,18,46,64,59,41,81,82,22,78,36,47,17, 4, 6,17, 5,36]

pattern both : A........A.B.C..C.B      (exactly as reported)
C-anchor at offsets 13 and 16 : glyph 17 on BOTH sides
```

A lettered cell with the same glyph on both sides collapses `q[x] − q[x] = w`
to `w = 0`. Every other lettered cell then reads `q[u] − q[v] = 0`:

- offsets 0/9 → `q[64] = q[22]`
- offsets 11/18 → `q[68] = q[36]`

**This is exactly right, and it explains something FR155 reported without
understanding.** I found `q[22]=q[64]` and `q[36]=q[68]` as *invariant across
all ~30 maximal readings* and called them the sharpest external tests. **They
are invariant because a single fixed-point alignment forces them outright** — no
sampling argument needed. The correspondent supplied the mechanism I was missing.

And the alignment is neither E1@68 nor E3@101, so **it survives both repairs**.
Bijectivity is gone with or without T1, which is a cleaner statement than
FR127's exhaustion argument.

---

## CLAIM 3 — fixed-point-free families still force homophones. **OBSERVATION CONFIRMED. INFERENCE NOT.**

Dropping every family containing an identical-glyph cell:

```
208 classes, 49 with a fixed point, 159 clean
143 accepted -> 657 relations, 59 glyphs, FORCED EQUALITIES: 5
   (2,16) (4,60) (10,75) (19,35) (37,66)
```

**Five forced pairs from fixed-point-free evidence — the same count reported,
though different pairs** (their 17 families vs my 143; different selections
reach different pairs).

**But the control does not replicate.** Running the identical pipeline on
synthetic corpora generated by our own cipher — linear `K`, per-message bases,
planted repeat passages, **injective `C` with no homophones at all**:

```
synthetic 1 : 117 classes, 117 clean, FORCED EQ 6
synthetic 2 :  85 classes,  85 clean, FORCED EQ 8
synthetic 3 : 115 classes, 107 clean, FORCED EQ 9
REAL CORPUS : 208 classes, 159 clean, FORCED EQ 5
```

> **The pipeline forces 6–9 homophones on corpora built with no homophones in
> them. The real Eye forces five — fewer than the synthetic controls.**

Forced equalities are a **method artifact**: an over-determined linear system
over GF(83) produces coincidental equalities whether or not the underlying `C`
is injective. The reported 0/3 does not reproduce here, and the discrepancy must
lie in the generator — worth reconciling, since the inference `d ≡ 0` rests
entirely on it.

**Direct check:** the canonical model builds at **all 82 nonzero drifts**. No
`d = 0` forcing is present in this system.

---

## CLAIM 4 — the opening versus `base(E2) − base(E1) = 77`. **CORRECT, AND THE MODEL'S ANSWER SHOULD BE STATED PLAINLY.**

```
positions 0-24 where East 1 and East 2 are byte-identical : 24/25
positions where ALL THREE T1 messages agree               : 24/25
```

If any identical opening cell were co-plaintext, `q[x] − q[x] = 0` would force
`base_E2 − base_E1 = 0` against the forced 77, and `77d ≡ 0 (mod 83)` with 83
prime gives `d = 0`.

**So the model requires the openings to be non-co-plaintext, and here is the
explicit statement the critique asks for:**

> **The T1 opening (positions 1–24) and T3 opening (1–20) carry LITERAL glyphs —
> a stamped header written directly rather than encrypted text. They are
> excluded from every alignment the model uses.**

Verified: **0 pool alignments touch a T1 opening position.** The exclusion is
implemented, not merely assumed. But the correspondent is right that this is
load-bearing: **if those 24 identical glyphs are encrypted co-plaintext, the
progressive model is dead**, and FR150 already found 15 opening-overlapping
classes contradicting it, which is the same fact seen from the other side.

---

## CLAIM 2 — the E1@68 pricing. **NOT YET VERIFIED**

The claim is that extending E1@53 ↔ E2@65 keeps the column map bijective over
25 symbols, so E1@68 is not a 9-window and the 10⁻⁵·⁸ price is computed for the
wrong object. This needs the extension checked cell by cell and the FR128 cost
recomputed against the extended span. **Deferred to the next cycle rather than
answered from intuition.**

Note that FR150 found **East 3@101 is now inert** and FR156 records the
acquisition figure as 6 anchors, so the repair pricing has moved since FR128
regardless of how this resolves.

---

## Doctrine changes

| item | prior status | status now |
|---|---|---|
| `q[22]=q[64]`, `q[36]=q[68]` | invariant across ~30 readings (FR155) | **forced outright** by the W1@59↔E3@90 fixed point |
| Why they are invariant | unexplained | **explained** — a single fixed-point alignment |
| Bijectivity | dies with T1 coverage (FR127) | **dies regardless** — the fixed point survives both repairs |
| Forced homophones as evidence | treated as data | **METHOD ARTIFACT** — synthetic controls force 6–9 with none planted |
| `d ≡ 0` | proposed | **not supported here** — model builds at all 82 nonzero drifts |
| The openings | implicit doctrine | **stated explicitly**; 0 pool alignments touch them |
| E1@68 pricing | 10⁻⁵·⁸ | **challenged, unverified** |

---

## Model status

Unchanged pending Claim 2: extended skeleton, 794 relations, 61 glyphs, 8
homophones, 79.1% exposure, 435-position reading, 6 anchors, one of ~30 maximal
readings, 18 invariant relations. Cumulative: 27.16 billion candidates, zero
survivors.

---

## Horizon

1. **Verify Claim 2** — the E1@53 ↔ E2@65 extension and the recomputed price.
2. **Reconcile the synthetic generators.** Their control gives 0 forced pairs,
   mine gives 6–9 on the same nominal cipher. One of the two generators is
   wrong, and the `d ≡ 0` inference depends on which.
3. **Add the five fixed-point-free pairs to the anchor test** as the critique
   suggests — but as *candidate* tests, since the artifact result means they may
   carry no information.
