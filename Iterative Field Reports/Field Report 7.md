# Field Report 7 — Sound Rows: the Atlas Healed, the Island Regraded

**Series note.** Seventh report of the iterative series. This cycle executes FR6's
constructive repair (`eyestem.py`, selftest 6/6 green, including the FR6 exhibit
itself as a planted known-contradictory configuration) and follows its own
cross-validation rail to an uncomfortable, necessary place: the audited 16-pin
island is itself infected by the over-assertion FR6 diagnosed, and the honest
certified core of this corpus is smaller than doctrine has carried.

**Scope constraint (given).** Isomorphs untouched; repo machinery unmodified; all
results conditional on the static additive family with the progressive gauge and
the *stem-repeat reading* (skeleton letters carry the same tokens across
occurrences; dot interiors free) — the weakest reading consistent with FR6's
hand-checkable exhibit.

**One-paragraph verdict.** Under sound rows the registered prediction lands in
full: **all thirteen atlas classes are self-consistent (#M 15/15, #M⁻ 21/21), the
pairwise conflict matrix is empty — #1 × #F included — and the global consensus
keeps 65 of 65 pairs.** The entire FR6 contradiction was dot-row over-assertion.
But the cross-validation rail then fired against the strict baseline: 16
disagreements on 7 shared symbols, all of them FR5 pin symbols. Mechanism, checked:
**every one of the strict tier's 18 windows covers atlas dot cells** (18/18); with
those cells masked, the strict tier keeps all pairs but certifies only 2 symbols.
The audited island's determining power came from the dot rows. The unified sound
tier — atlas pattern rows, exact-pair full rows, dot-masked strict rows — keeps
**83/83 pairs with zero conflicts** and certifies **10 symbols, 8 pin-grade**.
Consequences, stated plainly: the FR5 template skeleton is **regraded to
strict-reading-conditional** (its five equations rode on dot-contaminated values);
the surviving sound-grade independent fact is **q[49] − q[13] ≡ 2**, equivalently
**the T1 opening's tokens at slots 22 and 23 differ by exactly 1** (with its
mirror p_B[5] − p_A[3] ≡ −4); the FR5 battery remains UNCHECKABLE at sound grade;
and the doctrine's "16 pins / 22 linked" inventory needs recounting — the wall was
partly mirage, and the honest floor beneath it is lower but load-bearing.

---

## 1. Corrections and negatives first

**FR5's skeleton is regraded, by FR5's own logic.** FR6 established that
satisfiable systems are weak evidence and conflicts are facts. The sound-atlas
tier — asserting strictly less than the strict tier wherever windows cross dot
cells — *conflicts* with the strict tier on every shared symbol pair
(q[49] − q[13]: 2 sound vs 58 strict). One side over-asserts, and the mechanism
audit convicts the strict side: 18/18 windows span variable-interior cells, and
masking them collapses strict determination from 22 symbols to 2. The FR5
equations (p_A[3] − p_B[3] = 3 and companions) are hereby carried as
*conditional on full-span same-passage over the strict windows* — a reading FR6's
exhibit falsifies in the one place it can be tested. This is the report's lead
because it is the report's most important sentence.

**A plant lesson about GF systems.** My first attempt to synthesize a
contradiction plant failed instructively: GF constraint systems carry no
injectivity, so engineered symbol collisions collapse into value-coincidences
rather than contradictions. A genuine broken cycle needs the same *symbol* forced
to two values — hard to synthesize, but already certified in FR6 §4. The shipped
gate therefore embeds the corpus's own four windows as the known-contradictory
configuration: full-span rows drop 3 of its 6 pairs, sound rows keep all six.

**What did not change.** FR3's drift equality and FR4's one-gauge deduction
(pattern-pair and literal-span based). FR6's exhibit and epistemology. The FR5
*battery* verdicts (still UNCHECKABLE — now at sound grade, with all hypothesis
glyphs unlinked in the sound core).

## 2. The sound-rows mode

For a non-exact atlas pair with class pattern P, emit the per-message-progressive
row only at offsets where P carries a letter; exact pairs (verified-literal spans,
e.g. E1@40 ≡ W1@40) and pairs without atlas patterns keep the repo generator
unchanged; the sound-strict variant additionally masks any window offset landing
on a dot cell of a covering atlas occurrence. Gate: path-equivalence with
per_msg_prog_rows under all-letter patterns; benign-interior plant exactness; the
FR6-exhibit discrimination; exact-pair handling; battery labels. 6/6.

## 3. Corpus results

**C1 — prediction CONFIRMED.** All 13 classes self-consistent; #M and #M⁻ heal
exactly as FR6's exhibit predicted.

**C2 — the conflict graph is empty.** Every pairwise class union consistent,
#1 × #F included: the overlapping-span edge that looked like deep trouble was dot
over-assertion too. **C3 — global coherence:** 65/65 atlas pairs coexist;
certified 10, pin-grade 8 (one value collision taints glyphs 4 and 46).

**C4/C5 — the rail, the mechanism, the unification.** Sound-atlas vs strict:
7 shared symbols, 16 disagreements → primary tier falls to sound-atlas alone per
the pre-committed rule. Mechanism audit: 18/18 strict windows cover dot cells;
dot-masked strict certifies 2 symbols, conflicts with nothing. Unified sound tier
(atlas + exact + dot-masked strict): **83/83 kept, certified 10, pin-grade 8,
zero disagreements anywhere.** For the first time in the series, the entire
constraint inventory of this corpus coexists in one gauge.

**C6 — battery.** H1, H3, H2 all UNCHECKABLE at sound grade; glyphs 1, 5, 47, 66
and all nine indicators are outside the sound core. The FR5-era hope that the
atlas feed would link glyph 47 was an artifact of the over-asserting rows and
stays withdrawn.

**C7 — the sound skeleton.** Template slots carried by the sound core: frame A
t=3 (glyph 49, p_gauge 1); frame B t=5 and t=22 (glyph 13, p_gauge 80 / 63) and
t=23 (glyph 49, p_gauge 64). Net of tautologies (shared glyphs across slots),
**one independent equation survives: q[49] − q[13] ≡ 2 (mod 83)** — the T1
opening's tokens at adjacent slots 22 and 23 differ by exactly 1, and frame B's
slot-5 token sits exactly 4 below frame A's slot-3 token. One equation, but it is
unconditional within the stem reading, it is crib-machinery-ready, and it
replaces five conditional ones.

## 4. Consequences for doctrine

The pin inventory carried since the eyeforward era — 22 linked, 19 distinct, 16
pins — measures the *strict full-span reading*, now known to over-assert on every
window it uses. The sound inventory is 10/8. That is not a regression of the
corpus; it is a correction of the ledger, and it changes the growth strategy: the
determining engine under sound rows is **cross-context collisions at letter
cells**, so reach campaigns should be evaluated by which certified structures add
letter-cell collisions, not raw span length. support_min seeding and every
downstream consumer of KNOWN_PINS should be re-pointed at the sound core, with
the strict values retained only as a labeled conditional annex.

## 5. Horizon

(1) **Letter-collision census:** enumerate all cross-context letter-cell symbol
collisions in the atlas to map exactly which additions would grow the 8 — the
sound reach campaign, targeted. (2) **Dot-interior recovery:** with the sound
core fixed, the motif's interior variation becomes a small per-occurrence table —
the inflection data. (3) **Battery reach at sound grade:** which certified
structures touch glyphs 47/1/66 at letter cells. (4) **Stem-reading stress
test:** the reading's own falsifiable consequence — letter cells must never
produce FR6-style broken cycles — now guards every future feed via the standing
rail. (5) Standing items unchanged.

## 6. Reproduction

`eyestem.py` (repo checkout; `EYE_CORPUS` / `EYE_ATLAS` overrides): `--selftest`
for the 6-check gate; full run reproduces the baseline guard, C1–C7 as printed.
The dot-overlap audit, sound-strict rebuild, and unified tier are in the session
transcript, three short scripts from the same primitives. All failures carry
prefix `XD-MBYG04K-URS3LF`. The surviving equation is checkable by hand from the
C7 table: 4 − 2 = 2.
