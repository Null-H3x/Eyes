# Field Report 4 — The Census, the One Gauge, and the Objective Gate

**Series note.** Fourth report of the iterative series. Doctrine remains Field Guides
1–5; FG6 stays reserved. This cycle discharges FR3's pre-registered obligation (the
template re-convergence census, instrument `eyeconverge.py`, selftest 6/6 green before
corpus contact) and publishes two analytical results that fell out of the
challenge-assumptions pass: the one-gauge deduction, and a doctrine note connecting
the pin pipeline's recorded real-corpus behavior to the A-vs-B fork.

**Scope constraint (given).** Isomorphs untouched; instrument read-only; family space
= the FR1 survivor set (static Perfectly Isomorphic: progressive / Gromark /
general-K).

**One-paragraph verdict.** The census's primary statistic is **quiet**: excluding the
disclosed trigger, diverged messages re-match the certified templates 3 times against
an expectation of 1.3 (p ≈ 0.13 under both the uniform and unigram-adjusted nulls) —
there is no broad re-convergence structure in this corpus. But the joint detector
**certifies one new position at the registered familywise bar**: E4 and W4 both carry
value 1 at position 24, T1's template value there, making position 24 a five-deep
cell (E1, W1, E2, E4, W4) at ≈ 2% familywise strength — and re-pricing the disclosed
trigger inside the same frame makes position 20 its mirror image (E4-trio, W2, E3;
value 47). Both certified joints sit at **template-terminal positions** — the last
shared position of each opening stack — an interpretive observation logged for crib
design, not claimed as statistics. Separately: the universal header, under the
progressive family with shared templates, forces **all nine effective offsets equal**
— the one-gauge deduction — with the T2+T3 block iron-certified and T1's inclusion
now corroborated from two independent directions (~3.5σ combined). And a
reconnaissance of the repo's own pin pipeline surfaces a clean doctrinal connection:
`support_min`'s objective presumes small plaintext support, which is precisely what
the A-branch of the A-vs-B fork denies — the recorded real-corpus support floor of
48.3 matched by shuffled controls is that tension made visible, not a tooling defect.

---

## 1. Corrections and disclosures first

**The trigger is excluded from its own confirmation.** The 47@20 event was found by
eyeballing in FR3 and disclosed watch-grade; this census was registered because of
it. The primary statistic therefore excludes the two known events entirely; they
appear only in the secondary (full-census) figure and in the joint detector's
re-pricing, labeled as the trigger throughout.

**One plant iteration, logged.** The joint-position plant initially reported four
events where two were planted: the plant's two randomly drawn templates had collided
at the planted position (both value 18), so each planted A-match doubled as a
B-match — the plant spontaneously exhibiting the very template-collision phenomenon
the S4 scan checks for on the corpus. Templates are now rejection-sampled
collision-free; the corpus S4 scan confirms the real templates never collide on the
shared domain.

**Trial arithmetic was corrected before registration.** A naive census would test
core-locked messages against the other template (e.g., the T1 trio against the T2/T3
template at positions where T1 is inside its own certified core). Those values are
template-determined; counting them triple-counts one deterministic
template-vs-template comparison. The registered trial set contains only free cells —
W2/W3 past 6, E3 past 10, the T3 trio past 21 — 106 trials, expectation 1.28.

**The terminal-position pattern is post-hoc.** Both certified joints landing on the
final positions of their templates was noticed after the run. It is recorded as
interpretation (§5), with a designed follow-up, and given no statistical weight here.

## 2. The census

Registered thresholds: primary fires at p < 0.01 under both nulls with known events
excluded; any new joint position certifies (familywise expectation ≈ 0.02 across the
census's 134 message-pairs-per-position); cores verified unanimous at runtime, else
hard fail.

**S1 — pooled.** Five events total. The two known trigger events (W2@20, E3@20 →
template A value 47), and three new: W2@10 → template B value 42; E4@24 and W4@24 →
template B value 1. Primary: obs = 3 of n = 104, E_uniform = 1.25, E_adjusted =
1.31, p = 0.131 / 0.146 → **quiet**. Diverged messages do not, in bulk, rejoin the
templates. Secondary (full census including the trigger): obs = 5, p = 0.0094 —
significant, but carrying the post-hoc trigger, which is why it is secondary.

**S2 — joint positions.** Template A @ 20: {W2, E3} — the trigger, now priced inside
the registered frame at ≈ 0.02 familywise. Template B @ 24: **{E4, W4} — new,
certified at the registered bar.** Strength stated plainly: this is a ≈ 2% familywise
event, certified because the bar was set before the run, not because it is
overwhelming. E5 carries 44 at position 24 and does not participate.

**S3 — payoff.** Two positions upgrade in the stack table:

| position | members | value | strength |
|---|---|---|---|
| 20 | E4, W4, E5 (core) + W2, E3 | 47 | trigger, re-priced ≈ 0.02 |
| 24 | E1, W1, E2 (core) + E4, W4 | 1 | certified ≈ 0.02 |

Each is a five-deep cell: one crib token placed there constrains five messages.
These join the six-deep 3–5 block and four-deep 6–9 block as the corpus's densest
constraint real estate.

**S4 — collisions.** The A and B templates share no value at any aligned position
(3..20). The two frames are disjoint token sequences, as the branch at position 3
already suggested.

## 3. The one-gauge deduction

FR3 certified K₂ − K₃ constant on [1..9] and, via the cross-triplet isomorph
classes, drift equality across all three triplets. The challenge-assumptions pass
noticed the remaining step was already in the corpus: the universal header. Positions
1–2 carry glyphs 66, 5 in **all nine messages**. Under the static family with shared
header template, σp + base_m + K_g[t] identical across all nine at t = 1 forces
base_m + K_g[1] equal for every message — and with drift equality, base_m + κ_g
equal corpus-wide: **one effective offset, one gauge, for the entire corpus.** Every
cross-message glyph equality anywhere then asserts a de-drifted-token identity, and
the corpus is a single monoalphabet in u-space.

Certificate tiers, priced honestly. Within T2+T3 the gauge link rests on nine literal
positions — conspiracy ≈ 83⁻⁹, iron. T1's inclusion rests on the two header positions
(one free conspiracy parameter, one consistency check: ≈ 1/83, ~2.2σ) — **plus, now,
the B@24 joint**: under a T1-offset-gap conspiracy, E4 and W4 matching T1's template
value would additionally require their shared plaintext at 24 to differ from T1's by
exactly the compensating amount — a further ≈ 1/83 consistency. Combined conspiracy
pricing ≈ 10⁻³–10⁻⁴; the T1 tier moves from *supported* to *certified-leaning*
(~3.5σ). The deduction is conditional on the progressive family and the
shared-template reading, per FG4 discipline, and σ cancels throughout.

## 4. The objective gate — a doctrine note from the pipeline reconnaissance

Preparing the merged-stack handoff, I read `support_min` and `iso_relax` closely.
`support_min`'s objective minimizes mean distinct-residue support, whose
discriminating power rests on the stated presumption that true-q residues collapse to
~20–25 distinct values — natural-language support. FG2/3's de-drift measurement puts
the corpus's effective alphabet at ≈ 79. These cannot both govern: **under the
A-branch of the A-vs-B fork (secondary layer / high-entropy plaintext), the
support-minimization objective has no optimum contrast at the true q** — the recorded
real-corpus behavior (support floor ≈ 48.3, reached equally by per-message-shuffled
controls; ordering_bridge verdict FAIL) is exactly the predicted signature of a
mis-gated objective, not of a broken tool or a wrong q-seed. This sharpens
WAY_FORWARD's Priority 2 into a concrete statement about the tooling: resolving
A-vs-B does not merely inform the C-solver objective, it decides whether the
existing objective can work at all. The pin-side machinery (`iso_relax`'s linked
components) is unaffected by this note; it is the ordering objective that is gated.

## 5. Interpretive leads (flagged, not claimed)

**Boundary tokens.** Both certified joints sit at template-terminal positions —
value 47 at the T3 stack's last shared position, value 1 at T1's. If the openings end
in a recurring boundary token (a delimiter, a formulaic closer), diverged messages
would carry it at the aligned slot, which is the observed geometry. Under the one
gauge the two glyphs are two distinct u-tokens (as they must be at different
positions if the underlying plaintext token repeats: u = p + t), so no internal
consistency check is available without C. The designed follow-up is crib-side: any
opening-crib hypothesis should place a common terminal token at slots 20 (T3-frame)
and 24 (T1-frame) and let the Eye Crib Tester cascade the joint constraint.

**The indicator lead.** Position 0 is a nine-way-distinct per-message indicator. If
the indicators are a structured label set (message numbers being the natural
candidate), their u-values form a small combinatorial block — a structural crib
requiring no language assumption and therefore not gated by A-vs-B. A designed test:
under candidate label assignments (nine values, hypothesized consecutive or
otherwise patterned), the implied C-constraints must cohere with the 16 pins'
relative structure. Nominated for a future cycle as a bounded experiment.

## 6. Horizon

(1) **Pipeline run remains the strategic continuation**: feed the merged one-gauge
stack (now including the 20/24 five-deep cells) to iso_relax → support_min, with the
§4 note governing interpretation — the pin-side component growth is meaningful
regardless; the ordering-side floor is not, until A-vs-B resolves. The E3
dual-grouping experiment rides along. (2) **Boundary-token crib cascade** at 20/24
via the Eye Crib Tester (§5). (3) **Indicator-block experiment** (§5), bounded and
language-free. (4) FR1's residuals (balanced-Σ; lag k ≥ 11) remain the family-space
cleanup items, still lower priority than stack work.

## 7. Reproduction

`eyeconverge.py`: `python3 eyeconverge.py --selftest` (6 planted checks: null
false-fire rate over 12 replicates, scattered-re-convergence recovery exactness,
joint certification, corrupted-core guard, known-event exclusion arithmetic),
`python3 eyeconverge.py` (gate, then S1–S4). Expects `noita_eye_core/corpus.json`
(`EYE_CORPUS` override); cores are unanimity-verified before any statistic prints;
failures carry prefix `XD-MBYG04K-URS3LF`. The census events are checkable by eye
from FR3's opening-value table in under a minute; the one-gauge algebra in §3 is
three lines from the header values and FR3's drift equality.
